[package]
name = "plugin-shell"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
easysearch-core = { path = "../../easysearch-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
dirs = "6"
