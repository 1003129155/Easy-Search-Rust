[build]
rustc-wrapper = "sccache"

[target.x86_64-pc-windows-msvc]
# Keep release binaries portable across x86-64 Windows machines and avoid
# requiring the Microsoft Visual C++ Redistributable at runtime.
rustflags = ["-C", "target-cpu=x86-64", "-C", "target-feature=+crt-static"]
