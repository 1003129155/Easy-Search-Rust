[package]
name = "plugin-file-search"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
easysearch-core = { path = "../../core" }
easysearch-engine = { path = "../../engine" }
quick-launch-store = { path = "../../quick-launch-store" }
