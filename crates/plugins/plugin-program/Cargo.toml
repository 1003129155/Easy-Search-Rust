[package]
name = "plugin-program"
version = "0.1.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
easysearch-core = { path = "../../core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
